### Downloading Items

    Empty directory is not downloading in the zip 
    @shashank can create folders with same names -- only problem is displaying the folder multiple times
    @shashank sometimes photes are displaying on root folder even though they are no photoes in root folder
    @vardhan implement select to trash also
    @vardhan if possible implement drag to move items b/w folders
    @vardhan deleted flag in the space_items schema is not setting true after trashing it
    @all think about what to do with deleted items ( any deleted schema ) - items, spaces , etc.
        should we delete them with a time period? - not a potential fix
    @all we didn't face any upload errors as far but need to handle that in future
        
